# Copy to .env and fill the values
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name_here
VITE_CLOUDINARY_UPLOAD_PRESET=your_unsigned_preset_here

# Optional: signed uploads (requires running the signing server)
VITE_USE_SIGNED_UPLOADS=false
VITE_CLOUDINARY_SIGN_URL=http://localhost:4000/api/sign

# Server-side credentials (store these ONLY on the server, never in frontend source)
# Copy to server/.env
# CLOUDINARY_URL=cloudinary://<API_KEY>:<API_SECRET>@<CLOUD_NAME>
